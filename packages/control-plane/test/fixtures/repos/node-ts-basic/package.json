{
  "name": "node-ts-basic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "P5 的 fixture：npm + Makefile，没有 Dockerfile（应当命中 L3 信号级 → node-dev）",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
