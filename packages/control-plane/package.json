{
  "name": "@reuben-cloud/control-plane",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-timeout=60000 test/unit/*.test.ts",
    "test:integration": "node --test --test-concurrency=1 --test-timeout=300000 test/integration/*.test.ts",
    "test:live": "node --test --test-timeout=600000 test/live/*.live.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "@aws-sdk/client-s3": "^3.1131.0",
    "@aws-sdk/lib-storage": "^3.1131.0",
    "@octokit/auth-app": "^8.3.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1"
  }
}
